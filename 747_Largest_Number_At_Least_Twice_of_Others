def dominantIndex(nums):
    """
    :type nums: List[int]
    :rtype: int
    """
    if len (nums)>1 and sorted (nums) [-1] >= 2*sorted (nums) [-2]:
        return (nums.index (max (nums)))    
    elif len (nums) == 1:
        return 0
    else:
        return -1
